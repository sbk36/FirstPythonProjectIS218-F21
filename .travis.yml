language: python
python:
  - "3.9"
before_install:
  - cd App
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest --pylint --cov --cov-report=term-missing
#  - python -m  unittest discover -s  Tests

branches:
  only:
    - calc_file_handling_SK
    - calc_refactor_SK5
    - calc_Refactor_SK
    - calc_part_2_SK
    - master
    - development
